[1mdiff --git a/backend/api/services/llm_service.py b/backend/api/services/llm_service.py[m
[1mindex 1b369e5..ec01bec 100644[m
[1m--- a/backend/api/services/llm_service.py[m
[1m+++ b/backend/api/services/llm_service.py[m
[36m@@ -58,24 +58,52 @@[m [mclass LLMService:[m
             if self._model is not None:[m
                 return[m
             import torch[m
[31m-            from transformers import AutoModelForCausalLM, AutoTokenizer[m
[32m+[m[32m            from transformers import AutoModelForCausalLM[m
 [m
             device_map = "auto"[m
[32m+[m
             if self.device_pref in ("cpu", "cuda"):[m
                 device_map = {"": self.device_pref}[m
 [m
[31m-            self._tokenizer = AutoTokenizer.from_pretrained(self.model_id)[m
[32m+[m[32m            self._tokenizer = self._load_tokenizer()[m
             self._model = AutoModelForCausalLM.from_pretrained([m
                 self.model_id,[m
                 torch_dtype=torch.bfloat16 if torch.cuda.is_available() else torch.float32,[m
                 device_map=device_map,[m
             )[m
 [m
[32m+[m[32m    def _load_tokenizer(self):[m
[32m+[m[32m        """[m
[32m+[m[32m        Load the tokenizer for ``self.model_id``.[m
[32m+[m
[32m+[m[32m        Some checkpoints (e.g. ones saved with a bleeding-edge / dev build of[m
[32m+[m[32m        ``transformers``) write ``"tokenizer_class": "TokenizersBackend"`` into[m
[32m+[m[32m        their ``tokenizer_config.json``. Older ``transformers`` releases don't[m
[32m+[m[32m        know that class, so ``AutoTokenizer`` raises::[m
[32m+[m
[32m+[m[32m            ValueError: Tokenizer class TokenizersBackend does not exist or is[m
[32m+[m[32m            not currently imported.[m
[32m+[m
[32m+[m[32m        We first try the normal ``AutoTokenizer`` path, then fall back to[m
[32m+[m[32m        loading the fast tokenizer directly from ``tokenizer.json`` (which[m
[32m+[m[32m        bypasses the ``tokenizer_class`` lookup entirely while still picking up[m
[32m+[m[32m        the chat template and special tokens from ``tokenizer_config.json``).[m
[32m+[m[32m        """[m
[32m+[m[32m        from transformers import AutoTokenizer[m
[32m+[m
[32m+[m[32m        try:[m
[32m+[m[32m            return AutoTokenizer.from_pretrained(self.model_id)[m
[32m+[m[32m        except (ValueError, KeyError):[m
[32m+[m[32m            from transformers import PreTrainedTokenizerFast[m
[32m+[m
[32m+[m[32m            return PreTrainedTokenizerFast.from_pretrained(self.model_id)[m
[32m+[m
     @property[m
     def is_loaded(self) -> bool:[m
         return self._model is not None[m
 [m
     # ── Public generation methods ─────────────────────────────────────────────[m
[32m+[m
     def generate_simple(self, query: str, max_new_tokens=None, temperature=None,[m
                          use_context=True) -> dict:[m
         """[m
