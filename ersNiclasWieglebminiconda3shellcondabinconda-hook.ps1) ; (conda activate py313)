[1mdiff --git a/app.py b/app.py[m
[1mindex c7e1f64..98f7cd0 100644[m
[1m--- a/app.py[m
[1m+++ b/app.py[m
[36m@@ -87,37 +87,59 @@[m [mdef _configure_environment() -> str:[m
     # ── GPU detection at startup ──────────────────────────────────────────[m
     # Probe the hardware and set LLM_DEVICE accordingly, so the backend loads[m
     # the model onto the best available accelerator right from the start.[m
[31m-    # On T4 / A10G / L4 instances torch.cuda.is_available() is True at boot.[m
[31m-    # On ZeroGPU instances CUDA is only visible inside a @spaces.GPU context,[m
[31m-    # so we fall back to "cpu" here and let the backend's _resolve_device()[m
[31m-    # handle the fallback gracefully.[m
[32m+[m[32m    #[m
[32m+[m[32m    # ‼️ IMPORTANT: On HuggingFace ZeroGPU the ``spaces`` module patches[m
[32m+[m[32m    # ``torch.cuda.is_available()`` to return True in the host process, but[m
[32m+[m[32m    # CUDA is only real inside a ``@spaces.GPU``-decorated worker. Probing[m
[32m+[m[32m    # CUDA here and acting on the result (eager-loading the model) creates a[m
[32m+[m[32m    # CUDA context in the host that poisons ZeroGPU's fork-based workers,[m
[32m+[m[32m    # causing every generation request to fail with:[m
[32m+[m[32m    #     RuntimeError: No CUDA GPUs are available[m
[32m+[m[32m    # So we detect ZeroGPU FIRST and skip all torch probing.[m
     if "LLM_DEVICE" not in os.environ:[m
[31m-        try:[m
[31m-            import torch  # noqa: F811 (already imported above via spaces?)[m
[31m-        except ImportError:[m
[31m-            os.environ["LLM_DEVICE"] = "cpu"[m
[32m+[m[32m        is_zero_gpu = os.environ.get("SPACES_ZERO_GPU", "").lower() in ("1", "true", "yes")[m
[32m+[m[32m        if is_zero_gpu:[m
[32m+[m[32m            # ZeroGPU: CUDA is real only inside @spaces.GPU workers. Set[m
[32m+[m[32m            # "cuda" so _resolve_dtype picks bf16 for the model weights, but[m
[32m+[m[32m            # do NOT probe torch.cuda here — the patched probe lies and[m
[32m+[m[32m            # acting on it breaks the fork worker.[m
[32m+[m[32m            log.info("ZeroGPU runtime detected — setting LLM_DEVICE=cuda; "[m
[32m+[m[32m                     "the model is lazy-loaded inside the GPU context where "[m
[32m+[m[32m                     "CUDA is real (host-process probing skipped).")[m
[32m+[m[32m            os.environ["LLM_DEVICE"] = "cuda"[m
         else:[m
[31m-            if torch.cuda.is_available():[m
[31m-                props = torch.cuda.get_device_properties(0)[m
[31m-                log.info("GPU DETECTED: %s (%.1f GiB VRAM) — setting LLM_DEVICE=cuda",[m
[31m-                         props.name, props.total_memory / (1024 ** 3))[m
[31m-                os.environ["LLM_DEVICE"] = "cuda"[m
[31m-            elif getattr(torch.backends, "mps", None) and torch.backends.mps.is_available():[m
[31m-                log.info("GPU DETECTED: Apple Metal (MPS) — setting LLM_DEVICE=mps")[m
[31m-                os.environ["LLM_DEVICE"] = "mps"[m
[31m-            else:[m
[31m-                log.info("No GPU detected — model will run on CPU.")[m
[32m+[m[32m            try:[m
[32m+[m[32m                import torch  # noqa: F811[m
[32m+[m[32m            except ImportError:[m
                 os.environ["LLM_DEVICE"] = "cpu"[m
[32m+[m[32m            else:[m
[32m+[m[32m                if torch.cuda.is_available():[m
[32m+[m[32m                    props = torch.cuda.get_device_properties(0)[m
[32m+[m[32m                    log.info("GPU DETECTED: %s (%.1f GiB VRAM) — setting LLM_DEVICE=cuda",[m
[32m+[m[32m                             props.name, props.total_memory / (1024 ** 3))[m
[32m+[m[32m                    os.environ["LLM_DEVICE"] = "cuda"[m
[32m+[m[32m                elif getattr(torch.backends, "mps", None) and torch.backends.mps.is_available():[m
[32m+[m[32m                    log.info("GPU DETECTED: Apple Metal (MPS) — setting LLM_DEVICE=mps")[m
[32m+[m[32m                    os.environ["LLM_DEVICE"] = "mps"[m
[32m+[m[32m                else:[m
[32m+[m[32m                    log.info("No GPU detected — model will run on CPU.")[m
[32m+[m[32m                    os.environ["LLM_DEVICE"] = "cpu"[m
 [m
     # ── Lazy-load decision ───────────────────────────────────────────────[m
     # On dedicated GPU instances (T4, A10G, etc.) the GPU is always attached,[m
[31m-    # so we can eager-load the model at startup. On ZeroGPU, CUDA is only[m
[31m-    # visible inside @spaces.GPU, so we defer loading to the first query[m
[31m-    # (which runs inside the GPU context). Let the backend's own device[m
[31m-    # detection handle the switch once inside the GPU context.[m
[32m+[m[32m    # so we can eager-load the model at startup. On ZeroGPU, CUDA is only real[m
[32m+[m[32m    # inside @spaces.GPU workers, so we ALWAYS lazy-load — loading in the host[m
[32m+[m[32m    # process creates a CUDA context that poisons the fork-based worker,[m
[32m+[m[32m    # causing ``RuntimeError: No CUDA GPUs are available`` on every query.[m
     if "LLM_LAZY_LOAD" not in os.environ:[m
[31m-        gpu_always_available = os.environ.get("LLM_DEVICE") == "cuda"[m
[31m-        os.environ["LLM_LAZY_LOAD"] = "false" if gpu_always_available else "true"[m
[32m+[m[32m        is_zero_gpu = os.environ.get("SPACES_ZERO_GPU", "").lower() in ("1", "true", "yes")[m
[32m+[m[32m        if is_zero_gpu:[m
[32m+[m[32m            os.environ["LLM_LAZY_LOAD"] = "true"[m
[32m+[m[32m            log.info("ZeroGPU: lazy-load forced to prevent host CUDA "[m
[32m+[m[32m                     "context from poisoning fork workers.")[m
[32m+[m[32m        else:[m
[32m+[m[32m            gpu_always_available = os.environ.get("LLM_DEVICE") == "cuda"[m
[32m+[m[32m            os.environ["LLM_LAZY_LOAD"] = "false" if gpu_always_available else "true"[m
 [m
     return os.environ["KG_STORAGE_DIR"][m
 [m
